<%= simple_form_for(@restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category %>
  <p>Seul ces catégories sont accèptées ["chinese", "italian", "japanese", "french", "belgian"].</p>
  <%= f.button :submit %>
<% end %>
